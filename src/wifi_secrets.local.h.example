#pragma once

// Optional local overrides for cluster WiFi proof builds.
// Copy this file to src/wifi_secrets.local.h and edit locally.
// The real local file is ignored by git; do not commit private credentials.

#define CLUSTER_WIFI_SSID "RI-ESP-CLUSTER"
#define CLUSTER_WIFI_PASSPHRASE "localfirstai"
#define CLUSTER_WIFI_UDP_PORT 42100
